<div id="game-over" >
  <h1>GAME OVER</h1>
  <br>
    <div id="end_game_stats">
      <ul>
        <% User.points.each do |user| %>
        <li>name: <%= user.name %> points: <%= user.points %></li>
        <% end %>
      </ul>
    </div>
</div>